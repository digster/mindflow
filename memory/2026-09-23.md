# 2026-09-23 — Stale icon note in ARCHITECTURE.md

Branch `claude/compassionate-euler-a8x9p5`. One docs-only commit, queued as a
follow-up during the previous session's work.

`ARCHITECTURE.md` ("Dependencies") still claimed the icons were hand-written SVG
paths in `src/ui/icons.ts`. That has been false since the Lucide move
(2026-08-26): the file is generated by `scripts/build-icons.mjs` from the
`lucide-static` devDependency through an element/attribute whitelist, committed,
and bannered `DO NOT EDIT BY HAND`.

Replaced the sentence with a paragraph covering: Lucide (ISC), extracted at build
time and inlined, so there is still no runtime third-party code and no CDN; how
to add an icon (manifest line + `npm run icons`); and the drift test in
`test/unit/icons.test.ts`. The wording follows `CLAUDE.md`'s workflow note and
the README's Credits section, so the three descriptions agree.

No code change and no `index.html` rebuild.

Left alone: the "Development:" line just above still omits `lucide-static` from
its list of dev tools. The new paragraph names it, so the section is accurate as a
whole, but the list itself is incomplete.

---

# 2026-09-23 (later) — Cmd+V pasted two copies

On `main`. One bug fix.

## The ask

> Selecting, copying and then pasting a selected object creates two copies of it.

## Cause

Two routes to `actions.paste()` for one keypress: the Cmd/Ctrl+V branch in
`input/keyboard.ts`, and the window `paste` listener in `app/app.ts`, which the
same chord triggers as its default action. The keyboard comment called them
"harmless duplicates" (and pointed at a `main.ts` that no longer exists). Both
copies land at the same +16 offset, so the second is invisible until moved.

Same root cause, second symptom: pasting a screenshot also pasted the board
clipboard, because the keydown route cannot see image data.

## Fix

`src/input/pasteGate.ts`, DOM-free, created in `app.ts` and shared by both
routes. The keydown now calls a new `onPasteShortcut` option instead of pasting.

- The native event takes the press. It is preferred because only it carries images.
- The keydown pastes on its own only if no native event arrives within 100 ms.
  This path is kept for WebKit, which fires no `paste` on a non-editable target
  unless `beforepaste` is cancelled (confirmed in WebKit's `EditorCommand.cpp`).
- A native event arriving within 1 s after the fallback has pasted is discarded.
- A second press inside the grace period supersedes the first rather than queueing.

The native listener's three `preventDefault` calls collapsed to one after the
typing-target guard.

## Tests

- e2e (`element clipboard`): one copy per Cmd+V; still pastes when native `paste`
  is swallowed (the WebKit simulation); a screenshot paste after a board copy
  inserts only the image. The first and third failed before the fix, with 3
  elements (two stacked at 116,116) and with an extra rectangle.
- 11 unit tests for the gate under fake timers, covering the arrival orders
  Playwright's Chromium cannot produce.
- 425 unit/contract and 145 e2e pass. Typecheck is clean.

## Docs

`docs/05-interactions.md` Clipboard section, `ARCHITECTURE.md` Interaction
section, `LEARNINGS.md` entry. No format change and no schema bump.

## Not done

- **A `beforepaste` listener that cancels on the board** would make WebKit fire
  native `paste` there too, bringing screenshot paste to Safari. It was not
  verifiable here: Playwright's WebKit runs key bindings with
  `allowExecutionWhenDisabled`, so it skips the menu validation that matters.
- **The native route still reads `navigator.clipboard.readText()`** instead of
  `event.clipboardData`, which it already holds without a permission prompt.

---

# 2026-09-23 (evening) — Recent-boards menu on the logo

On `main`. One feature, uncommitted.

## The ask

Click the logo beside the board name to reopen any recent board still stored
locally, in the spirit of the startup "Recover unsaved work?" prompt.

## What was there

`io/autosave.ts` stored ONE IndexedDB record under the key `current` and deleted
it on save, New board and Open. There was nothing to list. It also had a latent
bug: `clear()` did not cancel the 1.2 s debounce, so the write scheduled by
`load` landed after the clear. After New board or Open the next launch could
offer a blank or already-saved board as "unsaved work".

## Decisions (asked, all recommended options chosen)

1. List every recent board, not only unsaved ones. Capped at 10; saving clears
   an `unsaved` flag instead of deleting the copy.
2. Leaving a board keeps its copy. The leave prompt says so ("Leave unsaved
   changes?" / Continue) and falls back to the old "Discard" wording only when
   storage is refused or the board is empty.
3. Startup asks only about the board on screen when the last session ended, and
   only if it is unsaved. "Start blank" keeps it and does not re-prompt.

## Implementation

- `io/autosave.ts`: DB version 2 with three stores: `autosave` (contents, keyed by
  board id), `recent` (small summaries for the menu), `session` (`lastOpen`
  marker). The v1 `current` record migrates on upgrade. There is one cached
  connection plus a write queue for ordering. A snapshot of a different board
  flushes the pending one. Empty boards are deleted, not stored. Eviction prefers
  saved boards (`pickEvictions`, pure). Resolves on transaction `complete`.
- `ui/recentBoards.ts`: the popover. Rows show name, Open/Unsaved tags, count
  and relative time, plus a trash button. Arrow keys navigate and Enter presses
  whatever is focused. Unmodified keydowns stop propagating so they don't reach
  the `window` shortcuts (arrows were nudging the selection behind the menu).
- `ui/toolbar.ts`: `.mf-brand` became a button (`brandButton`) with the new
  callback `onRecentBoards`. There is also a palette command, "Recent boards…".
- `app/app.ts`: snapshots on document/load (debounced) and saved (immediate);
  `markOpen` on load; flush on `visibilitychange` hidden and on `beforeunload`.
  All `autosave.clear()` calls are gone. New `openRecentBoards`, `openRecent`,
  `loadRecent` (shared with startup) and `removeRecent`, which confirms only for
  unsaved boards and closes the popover first, since a modal and a popover
  fight over Escape. Recent boards reopen with origin `new`, never relinked to
  their file or Drive entry.
- `dialogs.ts`: `confirmDialog` gains `cancelLabel`; recovery dialog reworded.

## Tests

- 14 new e2e tests ("recent boards" block): listing and reopening, flush on
  switch, clean after save, blank never listed, removal, palette and keyboard,
  arrow-key isolation, 5 startup cases including the v1 migration, and storage
  refused (`indexedDB` getter throws).
- Updated 3 e2e tests: Discard→Continue ×2, and the reload test now polls the
  `recent` store and waits for the async recovery load.
- 12 unit tests in `test/unit/recentBoards.test.ts` (eviction, sort, time format).
- 437 unit and 159 e2e pass; typecheck clean; `index.html` rebuilt.

## Docs

`docs/06-persistence.md` (Autosave section rewritten as "Autosave and recent
boards", plus storage layout and migration), `docs/05-interactions.md` (a new
Recent boards section), README features, ARCHITECTURE (a new subsection), and
LEARNINGS (3 entries, plus a correction: Chromium DOES grant IndexedDB to
`file://`; the old entry said every browser blocks it). No format or schema
change.

## Not done / follow-ups

- The context menu has the same arrow-key nudge bug. Confirmed with a scratch
  script (y 100→102) and flagged as a separate task.
- Reopened boards are not relinked to their file handle or Drive id (deliberate:
  overwrite risk). Storing the handle and re-requesting permission is possible
  later.
- Two tabs on the same board: last writer wins. A second tab can be offered
  recovery of a board still open in the first (pre-existing behaviour).
- `.claude/launch.json` was created for the in-app preview (`npm run serve`); it
  is untracked.

---

# 2026-09-23 (night) — Popovers keep their keys to themselves

On `main`. One bug fix, uncommitted. This closes the follow-up flagged in the
recent-boards entry above.

## The bug

With the context menu open, ArrowDown moved the highlight *and* nudged the
selected shape 1px (y 100→102 after two presses). `installListNavigation` only
calls `preventDefault`. The window shortcut handler in `input/keyboard.ts` never
checks `defaultPrevented` and skips only typing targets, and menu buttons are
not typing targets. The same path let Backspace delete the selection, letters
switch tools and Space get swallowed by space-to-pan (so Space could not press
a menu button). The shape flyout had the same problem whenever focus was
inside it.

## Fix

- `ui/popover.ts`: the constructor adds a keydown listener on the root that
  calls `stopPropagation()` unless `metaKey || ctrlKey`, so every popover
  inherits it. This is always on, with no option: there is no popover whose
  keys should also drive the board.
- **Deliberately stricter than the old recent-boards version**, which also let
  `Alt` chords through. `keyboard.ts` ignores Alt on arrows and Delete, so
  Alt+↓ would still have nudged. `stopPropagation` does not block browser
  defaults, so letting Alt through gained nothing.
- `keyup` is not stopped, so space-to-pan can always end.
- `ui/recentBoards.ts`: removed the local listener and pointed its comment at
  `Popover`.
- The `installListNavigation` doc now says its target must be inside a Popover.

Palette and find bar: their inputs are typing targets, so the global handler
already ignored their keys. Now those keys stop at the popover root instead,
with no behaviour change. Their suites pass unchanged.

## Tests

Four new e2e tests, all confirmed failing against the pre-fix build except the
chord one, which is a guard against over-isolating:

- context menu: ArrowDown×2 + Backspace + `r` leave the sticky and the tool alone,
  while the highlight still lands on "Copy";
- context menu: Space runs the highlighted entry (Duplicate → 2 elements);
- context menu: Cmd/Ctrl+Z still reaches the app (it undoes the creation);
- shape flyout: with an option focused, `e` does not select the eraser and Space
  picks the shape.

437 unit/contract and 163 e2e pass (was 159); typecheck clean; `index.html`
rebuilt. Also checked the original repro (unfilled rectangle, right-click on
its outline) live in the browser pane.

## Docs

`docs/05-interactions.md` (a general rule under the shortcut rules, plus a
bullet in the context-menu section), `ARCHITECTURE.md` (a keyboard-routing
paragraph in Interaction), and `LEARNINGS.md` (rewrote the existing popover
entry's fix and added three details).

## Noticed, not changed

- **The command palette does not close on Cmd+K.** Its header comment says it
  "has to close itself", but nothing in `commandPalette.ts` handles Cmd+K. With
  the input focused, the keydown is a typing target, so `keyboard.ts` ignores
  it and the press does nothing. This predates this change and is unaffected
  by it.
- **Keyboard users cannot reach the shape flyout.** Opening it leaves focus on
  "More shapes", and the flyout is appended at the end of `<body>`, so Tab does
  not reach it. The new test focuses an option directly.
- **`npm run serve` overwrote the built `index.html`** with the unminified dev
  build (the in-app preview starts it). I rebuilt with `npm run build` after
  stopping it and re-ran e2e (163 pass). The artifact diff is 2 lines. Added a
  LEARNINGS entry.

---

# 2026-09-23 (late) — Bound-shape jitter, and focus anchors (format 1.6.0)

On `main`. Two bug fixes, uncommitted, meant to land as two separate commits.

## 1. A shape with an arrow attached vibrated while dragged

**Cause:** `computeSnap` excluded only the moving elements. A connector bound
to the moving shape is re-routed from it every frame, and its box ends `gap`
units from the dragged edge, inside the 6px snap radius. Each frame snapped to
the previous frame's arrow, so the shape ran ahead of the pointer (about 1.18
units per pixel), then lurched back.

**Fix:** `withBoundConnectors(document, ids)` in `input/binding.ts` returns the
ids plus every connector bound to them. The move gesture captures it at
pointerdown as `snapExclude` and passes it to `computeSnap`. Resize and rotate
never snapped, so they were not affected.

**Tests:** an e2e test creeps the target 1px per step and asserts it moves
exactly with the pointer (it failed before the fix with the 1.18 drift), plus
two unit tests. **Docs:** a paragraph in `docs/05-interactions.md` under Object
snap, and a LEARNINGS entry.

## 2. The arrow start collapsed to one spot on the source

**Cause:** a drop in the middle 70% of a shape became `auto`, which records no
position and aims through the centre, at the other target's centre when that
end is bound. Ends dropped near the outline became `fixed`, which is why the
target end looked right.

**Decisions (asked):** the Excalidraw-style aim-point anchor with a format bump,
not "pin to the outline with no format change". Drops near the centre still
snap to `auto`.

**Format 1.6.0:**
- `BindingAnchor` gains `{mode: 'focus', u, v}`. It casts the outline ray from
  the focus point toward the reference and takes the LAST crossing. With no
  crossing, or a reference equal to the focus point, it falls back to `auto`.
  The gap pushes along the ray, not away from the centre.
- The reference rule changed. A bound far end contributes its anchor's aim point
  (the centre for auto, `(u, v)` for focus and fixed) instead of always its
  target's centre. This only differs from 1.5.0 when the far end is `fixed`.
- The registry hook became `outlineIntersect(el, direction, origin): Point |
  null`. Rectangle: a per-wall parameter (`wallParameter`). Ellipse: a
  quadratic, taking the larger root. Diamond: now uses
  `polygonOutlineIntersect`, since it has no closed form off-centre. Polygons
  and solids pass the origin through. `polygonOutlineIntersect` returns null on
  a miss instead of falling back to the bounding box.
  `rayIntersectElementOutline` still falls back to the rectangle from the
  centre. The new `outlineCrossing` in `geometry.ts` handles any origin.
- `createBinding` has three zones: `|u−0.5|, |v−0.5| ≤ 0.1` → auto
  (`CENTRE_SNAP`); otherwise inside the 0.15 band → focus; otherwise → fixed.
- `bindConnectorEnds` (moved out of the controller): when both ends land on the
  same shape, only `fixed` ends bind. Before, an arrow sketched inside a frame
  collapsed to a dot at the frame centre (two autos). With focus it would have
  come out reversed.
- `normalizeAnchor` in `linear.ts` clamps focus `u, v` to [0, 1]. Unknown modes
  read as auto.
- Identity migration 1.5.0 → 1.6.0. New `mindflow-1.6.0.schema.json`, generated
  from 1.5.0 by a JSON round trip (byte-identical apart from the edits). New
  example `focus-anchors.mindflow.json`, whose points were generated by the real
  code through an esbuild-bundled scratch script. Node's strip-types mode cannot
  load `document.ts`, which uses parameter properties.

**Tests:** an e2e test (two arrows from different spots in A to one pinned spot
on B; both start on their drawn lines, then B moves). It failed on the old
build with `auto`. Plus about 30 unit tests: binding zones, focus resolution,
the gap direction, fallbacks, the reference rule, same-shape ends, the origin
cases for the ellipse, polygon and diamond (including a check against the 1.5.0
closed form), `outlineCrossing`, and load clamp and round trip.

**Docs:** `07-rendering.md` (resolution rewritten, a new "Casting a ray at the
outline" section, the reference rule plus a "Changed in 1.6.0" note),
`03-elements.md`, `05-interactions.md` (zones, same-shape rule), `01-overview`,
README and `docs/README` version links, and the CHANGELOG 1.6.0 entry with its
rationale. LEARNINGS has an entry, including the rounding trap in collinearity
tests.

**Totals:** 474 unit/contract and 165 e2e pass. Typecheck is clean.
`index.html` was rebuilt (9-line diff). Also checked in the browser pane over
`file://`: two arrows from different spots stay on their drawn lines, swing
correctly when the target moves, and log no console errors.

## Not done

- `connectorsToRefresh` and `connectorsBoundTo` in `binding.ts` branch on
  `element.type === 'line' | 'arrow'`, which violates the "no type branching
  outside render/shapes" invariant. This predates today and was left alone.
- Dragging an existing arrow's endpoint does not rebind it. Bindings are only
  made on creation, so focus anchors come only from drawing.
- `fixed` u/v are still not clamped on load (focus now is). This also predates
  today.

---

# 2026-09-23 (late night) — Connector and path checks go through the registry

On `main`. One refactor, uncommitted. This closes the first "Not done" item in
the entry above (`connectorsToRefresh` / `connectorsBoundTo` branching on type).

## Scope (asked)

The two named `binding.ts` sites were the tip of about 35 element-type
comparisons in 11 files outside `render/shapes/`. The user chose "connector +
path": the 8 `line || arrow` sites and the 3 `line || arrow || draw` sites.
Everything else is listed under "Not done" below.

## What changed

- `model/registry.ts`: `ElementCapabilities.connector` (new flag), plus two
  type guards next to `capabilitiesOf`: `isConnector(el): el is LinearElement`
  and `isPathElement(el): el is PathElement`. Both use `findDefinition` and
  answer `false` for unregistered types instead of throwing, matching the old
  `===` behaviour.
- **Why a flag, not `'startBinding' in el`** (explained in the registry
  comment): most callers ask "is this a connector?" (arrowhead controls, the
  two-point validation), not "does it have bindings?". A structural test would
  also match any future type that reuses the field name. `path && !bindable`
  cannot work because `draw` matches it.
- `connector: true` in `linear.ts` only; `connector: false` in the other 11
  capability blocks (`polygons.ts` and `solids.ts` share one block per family).
- Call sites: `binding.ts` ×2, `commands.ts` (delete clean-up), `actions.ts`
  (paste binding remap) and `document.ts` (validation) use `isConnector`.
  `transform.ts` ×2 (resize scaling points) and `document.ts` (coordinate
  rounding) use `isPathElement`. In `stylePanel.ts`, `anyLinear` plus a second
  `find` with an `Extract<>` cast became one `selected.find(isConnector)`, and
  `updateLinear` filters with it.

## Tests

- `contract.test.ts`: `connector` added to the required list. "Connectors are
  never bindable" is now keyed on the flag, not type names. New: "the
  connector and path flags match the fields they promise" creates one element
  per definition and checks that `points` is present iff `path`, and the
  binding fields are present iff `connector`. This is what backs the guards'
  narrowing.
- New e2e test in "connectors": a rectangle alone shows no connector rows; with
  a mixed selection they appear, report the arrow, and Elbow/Dot edit only the
  arrow. Nothing covered the style panel's connector rows before.
- Mutation checks: flipping `linear.ts` to `connector: false` fails 7 unit
  tests (binding, commands, document validation, the new contract test) and,
  after a rebuild, the new e2e test. Restored and rebuilt afterwards; the built
  file has exactly one `connector:!0`.
- 475 unit/contract (was 474) and 166 e2e (was 165) pass. Typecheck clean.
  `index.html` rebuilt (9-line diff). Checked in the browser pane over
  `file://`: an arrow bound rectangle→ellipse re-routes when the ellipse moves,
  the mixed selection shows Line shape / End arrow, and the console has no errors.

## Docs

`docs/03-elements.md` (a `connector` column in the capability matrix, a
definition bullet, and a note that the contract test enforces "not bindable"
by flag), `docs/09-extending.md` (the sample capabilities, a flag-table row,
and the guard requirements), `ARCHITECTURE.md` (a paragraph on the capability
type guards), and a `LEARNINGS.md` entry (throw-versus-false and lost
narrowing when converting type branches). No format change: capabilities are
not saved, so there is no schema bump and no CHANGELOG entry.

## Not done: remaining `element.type` branches outside `render/shapes/`

- **Frame:** `model/frames.ts` ×5, `ui/stylePanel.ts:203` (name row),
  `render/export.ts` ×2 (clip paths).
- **Image:** `render/images.ts` ×2, `app/actions.ts:192` (copy files),
  `model/document.ts` ×2 (file-reference validation).
- **Fillable:** `ui/stylePanel.ts:197` (`!draw && !line && !arrow && !text`).
  This needs its own flag; no existing flag combination matches.
- **Text autoWidth:** `ui/textEditor.ts` ×3.
- **Draw validation:** `model/document.ts:468` (`draw` needs ≥ 1 point).
- **Table UI:** `ui/contextMenu.ts:67`, `ui/stylePanel.ts:212`. This needs a UI
  hook on the definition.
- **SVG exporter:** `render/export.ts:393` is a whole `switch (element.type)`.
  This needs a per-type `toSvg` hook, which is a large refactor.
- Not violations (not element types): `controller.ts` switches on the *tool*;
  `imageImport.ts` (MIME), `drive/auth.ts` (OAuth error),
  `document.ts:319` (board kind) and `migrate.ts:179` (a typeof guard).

---

# 2026-09-23 (later still) — The remaining predicate-shaped type branches

On `main`, on top of `0a907c0` (the connector/path refactor). Uncommitted.
This closes the follow-up list in the entry above, except the three larger
refactors, which stay out of scope.

## Decisions, per group

The dividing line (also in ARCHITECTURE and LEARNINGS): a fact that is the same
for every element of a type is a **flag**. One that depends on the element's
fields, or that carries type-specific logic or wording, is a **hook**.

| Group | Form | Why |
|---|---|---|
| Frame (8 sites) | flag `frame` + guard `isFrame(el): el is FrameElement` | Per-type role; the style panel needs `name`. Named after the format's own word (`frameId`); the `text` capability already shares a name with a type. |
| Image (5 sites) | flag `file` + guard `hasFile(el): el is ImageElement` | Every caller reads `fileId`, so `'fileId' in el` asks the right question. But **system-clipboard paste takes elements verbatim** (no normalisation), so a structural check could match a stray field where `type === 'image'` never did. A registry lookup cannot. |
| Fillable | flag `fillable`, no guard | Per-type and no fields behind it, so it is read through `capabilitiesOf` (already computed in that function). |
| Text wrapping | hook `wrapsText?(el)` | Depends on `autoWidth`, so it cannot be a flag. The default (wraps) lives in one private helper in `textEditor.ts`. |
| Text resize | hook `withText?(el, text)` | Mirrors `withRegionText`. `text.ts` re-measures; no hook = box unchanged (sticky). `textEditor.ts` no longer imports `measureTextElement`. |
| Draw validation | hook `validate?(el): string[]` | `path && !connector` would identify `draw` by flag arithmetic, which is the LEARNINGS anti-pattern. The rule and its wording now live in `draw.ts`. |

Other details:
- The missing-file message's noun now comes from the definition title ("Image"
  today, so the output is identical).
- The frame name row became `selected.length === 1 && isFrame(first)`, which is
  equivalent and drops a cast.
- `image` keeps `fillable: true` although it never paints `style.fill`: the
  panel offered it before, and the task said no behaviour change. It is
  documented as a UI-only flag.

## Tests

- Contract: the three flags join the required list. "Frames are never
  rotatable" is new; the clip is a plain rect in both renderers. The fields
  test is renamed "the narrowing flags match the fields they promise" and now
  covers `file` (both directions) and `frame` (one-way: `name` is not
  frame-specific). New: **the capability matrix is checked cell by cell**
  against the registry, where before only rows were checked. It failed before
  the docs were updated.
- Unit: the exact image-validation message; draw with 0 points reports once,
  1 point reports nothing, and a pointless connector gets only its own message;
  `withText` equals `measureTextElement` exactly (autoWidth and fixed width);
  `wrapsText`; sticky has neither hook; guards recognise their own types, answer
  false for unregistered types, and ignore stray pasted fields.
- e2e: the Fill rows appear only for fillable selections (arrow: none;
  rectangle and mixed: Fill + Fill style). Nothing covered this before.
- Mutation checks (backed up to the scratchpad and restored, not `git
  checkout`): wrapsText→true (1 fails), draw validate off (1), image
  `file: false` (4), frame `frame: false` (10), connectors fillable (1 unit
  test and, after a rebuild, the new e2e test).
- 485 unit/contract (was 475) and 167 e2e (was 166) pass. Typecheck clean.
  `index.html` rebuilt. Built flags: exactly one `true` each for
  connector/frame/file, and 9 `true` / 3 `false` for fillable.
- Browser pane over `file://`: an autoWidth text element grew 120→286 while
  typing with the editor at `white-space: pre`; a sticky stayed 150×130 with
  `pre-wrap` and wrapped. No console errors.

## Docs

`docs/03-elements.md` (three matrix columns, three bullets, and the
frames-never-rotatable note), `docs/09-extending.md` (the sample, three
flag-table rows, a guard/narrowing table, and new "Text that sizes itself" and
"Type-specific validation" sections), `ARCHITECTURE.md` (the hook list, the
flag-vs-hook rule, a guard table, and the three remaining branches), and
`LEARNINGS.md` (renamed test reference, paste-verbatim note, flag-vs-hook).
No format change: no schema bump, no CHANGELOG entry.

## Remaining (out of scope, as instructed)

- `render/export.ts:393`: the SVG exporter's `switch (element.type)`. This
  needs a per-type `toSvg` hook.
- `ui/contextMenu.ts:67` and `ui/stylePanel.ts:209`: table row/column UI. This
  needs a UI hook on the definition.

## Noticed, not changed

- `image` gets Fill / Fill style controls that do nothing visible. Setting
  `fillable: false` on image would fix it (a one-line behaviour change).
- `Actions.paste` trusts system-clipboard elements verbatim: no normalisation
  and no unknown-type check. A crafted payload with an unregistered type would
  reach `document.elements`, where `getDefinition` throws. This predates today.
